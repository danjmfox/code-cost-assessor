/**
 * Fixture: MALFORMED Estimator — missing default export with required shape.
 * Used to test AC-2.2: --estimator with invalid module exits 1 with descriptive error.
 */
export const notAnEstimator = 'this is not what you are looking for'
